To start the web app, open 2 terminal and run:
```
python start_proxy.py --server-ip 127.0.0.1 --server-port 8000
```
```
python start_login_app.py --server-ip 127.0.0.1 --server-port 8000
```
Then open a browser and access `127.0.0.1:8000/login`, `127.0.0.1:8000/register`
